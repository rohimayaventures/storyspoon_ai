# Example environment variables (use placeholders only)
# Copy this file to .env and replace with your actual values

# StorySpoon Server ENV
PORT=8787

# Provider (Google Realtime example)
PROVIDER=google
GOOGLE_API_KEY=your_google_api_key_here
MODEL=gemini-2.0-realtime-exp-02-2025
# Optionally override the base URL if needed by provider changes:
# GOOGLE_REALTIME_URL=wss://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-realtime?key=
